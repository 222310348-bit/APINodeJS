const Conversacion = require('../models/conversacion.models');
// se debe de dar de alta primero el modelo de clases
const Clases = require('../models/clases.models');

class ConversacionController {
    static async registarConversacion(req, red) {

    }
}